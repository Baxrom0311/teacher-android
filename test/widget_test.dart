import 'package:flutter_test/flutter_test.dart';

import 'package:teacher_school_app/main.dart';

void main() {
  test('TeacherApp can be constructed', () {
    const app = TeacherApp();
    expect(app, isA<TeacherApp>());
  });
}
